{
  "name": "tele-lytics-api-worker",
  "private": true,
  "description": "Cloudflare Worker shim in worker/index.ts — routes to the Python backend container and drives the pipeline worker's alarm. Not part of the Python package; see pyproject.toml for that.",
  "scripts": {
    "cf:typecheck": "tsc --noEmit",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/containers": "^0.0.29",
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "5.6.3",
    "wrangler": "^4.137.0"
  }
}
